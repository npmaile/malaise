# malaise-spec

The Malaise Language Specification, v0.9.snapshot-2026-09-03-UNSTABLE.

See [`SPECIFICATION.md`](SPECIFICATION.md).

Status of this document: authoritative except where it conflicts with the
reference implementation ([`../interpreter/`](../interpreter/)), the BDFL's
blog, or itself. In case of conflict the behaviour is implementation-defined,
which is defined as undefined.

The official documentation covers version 4. The current version is 7.
